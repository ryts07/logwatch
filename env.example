# Copier en .env - jamais commite (il est dans .gitignore).
# Chemins par defaut du script ; la ligne de commande (--log, --config, --reports) a priorite.
LOGWATCH_LOG=sample-logs.log
LOGWATCH_CONFIG=config.json
LOGWATCH_REPORTS=reports
